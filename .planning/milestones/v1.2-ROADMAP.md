# ROADMAP: Gamedemo 伪3D视觉改进 — v1.2 Milestone

**Project:** Gamedemo Pseudo-3D Visual Enhancement  
**Milestone:** v1.2 (Bug Fix)  
**Shipped:** 2026-04-01  
**Duration:** 1 day  
**Requirements:** 4/4 delivered  
**Final Commit:** f636811

---

## Overview

Fixed critical performance bug where FPS degraded as player moved away from spawn. Root cause was O(n) traversal of ever-growing resource/structure arrays.

**Solution:** Implemented frustum culling for all renderers + distant entity cleanup systems + cursor tracking fix.

---

## Phase 6: Scalability Fix

### PERF-09: Resource Frustum Culling ✅

**Implementation:**
- Modified `renderResources()` with early-out frustum culling
- Only processes resources within 40×40 tile view radius

### PERF-10: Structure Frustum Culling ✅

**Implementation:**
- Applied to all legacy renderers (`renderStructures`, `renderPlantedResources`, `renderDrops`)
- Created `calculateFrustumBounds()` helper (calculated once per frame)

### PERF-11: Distant Entity Cleanup ✅

**Implementation:**
- Created `DistantEntityCleanupSystem`
- Removes depleted resources >100 tiles from player (5x view frustum buffer)
- Runs every 300 frames (~5 seconds)

### PERF-12: World Tiles Optimization ✅

**Implementation:**
- Rewrote `renderTerrain()` to use view-based rendering (40 tiles radius)
- Added terrain sprite cleanup (>50 tiles → optimized to >80 tiles)
- Automatically re-creates sprites when returning to cleaned areas
- **Post-ship fix:** Optimized tile iteration from O(total_tiles) to O(view²) using lookup map
- **Post-ship fix:** Increased cleanup distance from 50 to 80 tiles to prevent pop-in

---

## Post-Ship Bug Fixes

### Cursor Tracking Fix ✅

**Issue:** Cursor didn't follow camera movement when player moved with stationary mouse

**Root Cause:** `pointer.worldX/Y` doesn't update with camera movement in Phaser

**Solution:** Use `camera.getWorldPoint(pointer.x, pointer.y)` to convert screen coordinates to world coordinates

**Commits:**
- `57e22bc` — Initial cursor fix attempt
- `f636811` — Use camera.getWorldPoint() for correct coordinates

---

## Files Changed

- `packages/engine-phaser/src/gameViewport.ts` — Frustum culling + terrain cleanup
- `mods/core-worldgen/src/distantEntityCleanupSystem.ts` — New cleanup system (49 lines)
- `mods/core-worldgen/src/index.ts` — Registered cleanup system

**Stats:** 5 files, +350 lines, -160 lines (including post-ship fixes)

---

## Success Criteria

- ✅ 500 tiles from origin @ 60fps — Implementation complete
- ✅ Memory usage stable — Cleanup systems active
- ✅ No breaking changes — All changes additive
- ✅ TypeScript compilation — Verified

---

*Archived: 2026-04-01*  
*Status: SHIPPED ✅*
